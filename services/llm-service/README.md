# LLM Service (Placeholder)

In the current stack, LLM is provided by **Ollama** via `docker-compose.yml`.

This folder is reserved for:
- Unified LLM API (OpenAI/Ollama/local runtimes)
- Token streaming
- Tool-call normalization
- Rate limiting / tracing

